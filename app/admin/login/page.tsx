import LoginForm from '@/components/LoginForm';

export const metadata = {
  title: 'Admin Login - Chat Web',
  description: 'Login to admin portal',
};

export default function LoginPage() {
  return <LoginForm />;
}
